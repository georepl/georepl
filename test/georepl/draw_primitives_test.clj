(ns georepl.draw-primitives-test
  (:require [clojure.test :refer :all]
            [georepl.draw-primitives :as dp]))

;;(deftest elems-test1
;;  (testing "draw-text-test"
;;    (is (nil? (#'dp/draw-text "Test" [0 0][250 30])))))


;(deftest quil-basics
;  (testing "text-width"
;    (is (= 10 (dp/text-width "B42")))))
